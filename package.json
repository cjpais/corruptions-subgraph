{
  "name": "corruption-messaging",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ cjpais/corruption-messaging",
    "create-local": "graph create --node http://localhost:8020/ cjpais/corruption-messaging",
    "remove-local": "graph remove --node http://localhost:8020/ cjpais/corruption-messaging",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cjpais/corruption-messaging"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.21.1",
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}
